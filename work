14	Registration + upgrade	https://subscription.rhn.redhat.com	443/tcp	Egress	Worker node Management IP
https://subscription.rhsm.redhat.com	443/tcp	Egress	Worker node Management IP
https://cdn.redhat.com	443/tcp	Egress	Worker node Management IP
https://solutionhub-metadata.s3.us-east-1.amazonaws.com	443/tcp	Egress	Worker node IP and EZ fab
https://gl-vas-harbor-prod-images.s3.us-west-2.amazonaws.com/	443/tcp	Egress	Worker node IP and EZ fab
15	Ezmeral	https://mirrors.fedoraproject.org	443/tcp	Egress	EZ fab
16	Enable preloaded examples in Airflow	https://github.com/HPEEzmeral	443/tcp	Egress	EZ fab and worker node IP
17	Enable the installation of python libraries not present in the existing images.	https://pypi.org/	443/tcp	Egress	EZ fab and worker node IP
kubectl get pods -A | grep -iv -e running -e completed
kubectl get pods -A | grep -E 'CrashLoopBackOff|Error'

•	systemctl status LsiSASH
•	systemctl stop LsiSASH
•	systemctl disable LsiSASH
•	systemctl restart minio
•	systemctl status minio

# sudo service kubelet stop
# sudo service containerd stop

curl -fL -o "kmod-megaraid_sas-07.732.03.00_rhel8u10-1.x86_64.rpm" https://downloads.hpe.com/pub/softlib2/software1/pubsw-linux/p1285309188/v260114/kmod-megaraid_sas-07.732.03.00_rhel8u10-1.x86_64.rpm

Upgrade the driver.
sudo rpm -Uvh --oldpackage --replacefiles --replacepkgs kmod-megaraid_sas-07.732.03.00_rhel8u10-1.x86_64.rpm

modinfo megaraid_sas | grep version

curl -fL -o "NVIDIA_H100_NVL_94GB_GPU_VBIOS-96.00.CE.00.0E-CEC-00.02.0192.0000.zip" https://downloads.hpe.com/pub/softlib2/software1/pubsw-linux/p2127639462/v274050/NVIDIA_H100_NVL_94GB_GPU_VBIOS-96.00.CE.00.0E-CEC-00.02.0192.0000.zip

unzip NVIDIA_H100_NVL_94GB_GPU_VBIOS-96.00.CE.00.0E-CEC-00.02.0192.0000.zip
sudo rpm -ivh NVIDIA_H100_NVL_94GB_GPU_VBIOS-96.00.CE.00.0E-1-0.x86_64.rpm
cd /usr/local/bin/NVIDIA_H100_NVL_94GB_GPU_VBIOS-96.00.CE.00.0E/  
sudo chmod +x NVIDIA_H100_NVL_94GB_GPU_VBIOS-96.00.CE.00.0E.scexe

sudo init 3
sudo bash ./NVIDIA_H100_NVL_94GB_GPU_VBIOS-96.00.CE.00.0E.scexe -f
sudo reboot
# sudo service containerd start
# sudo service kubelet start

export OP_ID=op-id    # Set this to the ezclustersvcs op-id / pcbe correlation-id
export OP=pre-stage   # Set this to the previous operation that was stuck, one of - pre-check, pre-stage, orchestrator-upgrade
export EXIT_CODE=1    # Set to 0 to mark the op as succeeded, 1 to mark op as failed

cat << EOF | kubectl apply -f -
apiVersion: batch/v1
kind: Job
metadata:
  name: clear-op
  namespace: ezkf-mgmt
  labels:
    hpe-ezfab/category: upgrade
    hpe-ezfab/component: $OP
    hpe-ezfab/cluster-type: orchestrator
    hpe-ezfab/op-id: $OP_ID
spec:
  ttlSecondsAfterFinished: 10
  backoffLimit: 1
  template:
    metadata:
      labels:
        hpe-ezfab/category: upgrade
        hpe-ezfab/component: $OP
        hpe-ezfab/cluster-type: orchestrator
        hpe-ezfab/op-id: $OP_ID
    spec:
      serviceAccountName: ezfab-registry-manager
      restartPolicy: Never
      containers:
      - name: main
        image: localhost:5000/ezmeral-common/ezkf/ezregtool:v1.2.3
        command: ["bash", "-c", "echo 'Job exiting with code $EXIT_CODE'; exit $EXIT_CODE"]
EOF

kubectl patch ezfabriccluster -n <cluster-name> <cluster-name> --type merge -p '{"spec":{"retryId":"<generate-a-random-uuid4>"}}'
